[build]
  # Directory that contains the deploy-ready HTML files
  publish = "." 
  # Directory that contains the serverless functions
  functions = "netlify/functions"

[[redirects]]
  # Redirect default Netlify function path to a cleaner URL (Optional but recommended)
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200